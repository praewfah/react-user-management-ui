# Optional for direct API calls (production/static hosting)
# VITE_API_BASE_URL=http://localhost:8000

# Used by Vite dev server proxy (recommended for local development)
VITE_PROXY_TARGET=http://localhost:8000

# Mock mode:
# - 0: use real API only
# - 1: use mock data only
VITE_MOCK_MODE=0
